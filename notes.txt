create subclasses (super.init())
When logging in, return "Admin" or "User" in that function and close the window
If type == "User":
    Kill the catalogue window
    Spawn a new window from the Logged_In subclass